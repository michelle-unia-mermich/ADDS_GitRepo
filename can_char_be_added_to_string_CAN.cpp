#include <iostream>
#include <string>

int main() {
    // Can we add a char to a string?
    char a = 's';
    std::string b = "ocket";
    std::string sum = a+b;
    std::cout << sum;

    return 0;
}